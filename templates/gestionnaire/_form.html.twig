{{ form_start(form) }}
    <div class="row">
        <div class="col-md-2">{{ form_row(form.email) }}</div>
        <div class="col-md-1">{{ form_row(form.roles) }}</div>
        <div class="col-md-2">{{ form_row(form.password) }}</div>
        <div class="col-md-2">{{ form_row(form.nom) }}</div>
        <div class="col-md-2">{{ form_row(form.prenom) }}</div>
        <div class="col-md-2">{{ form_row(formContri.phone) }}</div>
        <div class="col-md-1"><button class="btn btn-primary">{{ button|default('Enregistrer') }}</button></div>
    </div>
{{ form_end(form) }}